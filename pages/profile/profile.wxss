/* pages/profile/profile.wxss */
.container {
  padding: 24rpx;
  background-color: #fbf4e6;
  min-height: 100vh;
  box-sizing: border-box;
}

/* 用户信息卡片 */
.profile-card {
  display: flex;
  align-items: center;
  background-color: #fff7e7;
  padding: 40rpx;
  border-radius: 16rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(247, 180, 0, 0.12);
}

.profile-card.logged-out {
  justify-content: space-between;
  gap: 24rpx;
}

.avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  margin-right: 32rpx;
  background-color: #e9e9e9;
}

.avatar-ghost {
  opacity: 0.5;
}

.user-info {
  display: flex;
  flex-direction: column;
}

.nickname {
  font-size: 40rpx;
  font-weight: bold;
  color: #2a2a2a;
  margin-bottom: 8rpx;
}

.id-info {
  font-size: 28rpx;
  color: #666;
}

.login-btn {
  background-color: #f7b400;
  color: #fff;
  font-size: 28rpx;
  font-weight: 600;
  padding: 12rpx 24rpx;
  border-radius: 24rpx;
}



.arrow {
  font-family: sans-serif;
  margin-left: 8rpx;
  font-weight: bold;
}

/* 列表卡片通用样式 */
.action-list-card {
  background-color: #fff7e7;
  border-radius: 16rpx;
  padding: 16rpx 32rpx;
  box-shadow: 0 4rpx 12rpx rgba(247, 180, 0, 0.12);
  margin-bottom: 24rpx;
}



/* 操作列表 */
.action-list-card {
  padding: 0 32rpx;
}

.action-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 0;
  border-bottom: 1rpx solid #f2e5c8;
}

.action-item:last-child {
  border-bottom: none;
}

.action-text {
  font-size: 30rpx;
  color: #2a2a2a;
}

.action-picker {
  flex: 1;
  display: flex;
  justify-content: flex-end;
}

.action-value {
  display: flex;
  align-items: center;
  gap: 8rpx;
  color: #666;
  font-size: 28rpx;
}

.action-value-text {
  color: inherit;
}

.action-item.danger .action-text,
.action-item.danger .arrow {
  color: #cc4c54;
}